import { createStore } from 'vuex';
import AppModule from './modules/app.state';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    app: AppModule
  }
})
